.vs/
build/
/External/
**/_AutoGenerated/*
*.vcxproj.user
__pycache__/

# Linux build output. The existing lowercase 'build/' does not match on a case-sensitive
# filesystem, so without this git tracks the whole build directory.
Build/
compile_commands.json
.ninja_deps
.ninja_log

# Test content fetched by Docs/Linux/Scripts/FetchTestAssets.sh. Third-party assets and
# generated descriptors, deliberately not committed.
/Data/PortTests/
